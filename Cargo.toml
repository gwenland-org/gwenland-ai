[workspace]
members = [
    "packages/core",
    "packages/tui",
    "packages/mcp",
    "packages/gui/src-tauri",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
