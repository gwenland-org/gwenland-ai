[package]
name = "glcuda"
version = "0.1.0"
edition = "2021"
description = "GwenLand AI NVIDIA CUDA engine (stub — implemented in M2+)"
license-file = "../LICENSE.txt"

[dependencies]
glcore = { path = "../glcore" }
