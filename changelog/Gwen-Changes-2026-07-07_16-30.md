# GwenLand - 2026-07-07: GL Engine — Audit Fixes, Batched Prefill, llama.cpp Parity

**Date:** 2026-07-07 (WIB / SEAST)
**Scope:**
- Engine core: `glproc/src/{runner.rs,engine.rs,loader.rs,model.rs,threading.rs,sampler.rs,kv_cache.rs}`
- Kernels: `glproc/src/kernels/qdot/q8_0/{vnni.rs,avx2.rs}`, `glproc/src/kernels/dequant/q4_k/scalar.rs`
- Tokenizer/runtime: `glcore/src/{tokenizer.rs,runtime.rs,engine_trait.rs}`
- CLI: `glcli/src/main.rs`
**Type:** Correctness fixes (post-Deepseek-audit), prefill/load performance, diagnostics.
**Status:** Implemented, benchmarked, and committed this session (9 commits,
`85e6802..6257088` on `feature/m15-bridge-simd-threading`). 459 workspace tests green.

---

## Executive Summary

Three milestones landed in one session:

1. **M1.5 critical fixes** — the audit was right: generation never stopped at chat EOS
   tokens, small models looped without a repetition penalty, and the benchmark blended
   prefill into generation tok/s. All three fixed, plus ChatML prompt templating, which
   the clean-stop acceptance criteria turned out to require.
2. **M1.6 batched prefill** — prompts now run in 32-token chunks through batched matmuls
   with grouped row-dot kernels, and the real bottleneck (Q4_K `ffn_down` tensors falling
   into the f32-bridge fallback) was found by per-layer tracing and fixed with a
   Q4_K→Q8_0 load-time repack.
3. **M1.7 fast load (adapted)** — the token embedding table stays quantized (row
   dequantized per lookup) and layers load in parallel across cores. The megaprompt's
   raw-mmap `LayerPrefetcher` was skipped: its 0.05s-cold-start premise didn't match the
   codebase (load was ~2.6s), and dropping the repack would have regressed both prefill
   and decode. Deviation approved by the user before implementation.

End state on the dev box (i3-1115G4, dual-channel DDR4-2667, Windows 11, quiet machine):
**prefill 128–132 tok/s (llama.cpp pp24: 124.5), generation 33.5–35.2 tok/s (llama.cpp
tg128: 39.0), load 0.9s, peak working set 1.19 GB.**

Architecture and full benchmark detail: [`../ArchGLML.md`](../ArchGLML.md).

---

## What Changed

### 1. Stop tokens (`85e6802`)

The tokenizer resolves a stop set at load — the metadata EOS plus every known stop
marker present in the vocab (`<|endoftext|>`, `<|im_end|>`, `<|end|>`, `<eos>`,
`<end_of_turn>`, `</s>`, `<|eot_id|>`, `<|end_of_text|>`) — exposed as an O(1)
`is_stop_token()`. `Runner::generate` takes a stop predicate instead of a single eos id.
Stop tokens are never emitted to output.

### 2. Repetition penalty (`bb1a29b`)

`apply_repetition_penalty` shrinks positive logits (`/penalty`) and amplifies negative
ones (`*penalty`) for every token in the last 64 generated (llama.cpp's `repeat_last_n`
default), per occurrence, before each sample. Default 1.1; `--repeat-penalty` on the CLI;
1.0 disables. Looping is gone on 0.5B and 1.5B — and measured tok/s *dropped* when it
landed (23.3 → 15.2 on the same prompt), confirming the audit's claim that loops were
inflating throughput via L3-hot weight rows.

### 3. ChatML prompt template (`f8acc49`)

`Tokenizer::encode_chat` wraps single-turn prompts in ChatML, emitting
`<|im_start|>`/`<|im_end|>` as special-token ids (plain BPE would shred them into text
pieces). Applied automatically when the vocab has the markers; `gwen run --raw` opts out.
Without this, instruct models stayed in text-completion mode and rarely produced a stop
token, so the EOS fix alone could not make runs terminate.

### 4. Split benchmark metrics (`4c3c272`)

`Runner::generate` returns `GenTiming { prompt_tokens, prefill, decode }`;
`InferOutput` carries `prompt_tokens`/`prefill_ms`/`generation_ms`;
`GlEngine::stream` and `Runtime::stream` return the finished `InferOutput`; the CLI
prints prefill and generation rates separately.

### 5. SIMD path logging (`1a4c457`)

One startup line names the strategy and each hot weight class's kernel path:

```
[simd] strategy: Avx2+vnni256 | ffn gate/up: Q8_0 fused-swiglu integer-dot |
ffn down: Q8_0 integer-dot | lm_head: Q8_0 integer-dot
```

Caveat learned later this session: it samples layer 0 only, which masked the per-layer
Q4_K mix (see §7).

### 6. Batched prefill (`af12534`)

- `step_chunk` runs `PREFILL_CHUNK = 32` prompt tokens through all layers per pass.
- `par_matmul{,_qdot,_swiglu}`: threads own contiguous weight-row ranges; each row is
  dotted with every batch activation while cache-hot — weights stream from DRAM once per
  chunk instead of once per token.
- `row_dot_xn<G>` (VNNI + AVX2): one weight row against G=8/4 activations per call —
  block load, sign prep, and f16 scale conversion paid once per group, G independent
  accumulator chains for ILP.
- `KvCache` gained positional access (`write_k_at`/`write_v_at`/`read_k_to`/`read_v_to`/
  `advance_by`) for the layer-outer/position-inner batch loop; decode cursor semantics
  unchanged. Causality: all chunk K/V rows are written before any chunk position runs
  attention, and position `p` only reads rows `0..=p`.
- Chunk attention runs positions in parallel across the pool (it is quadratic in context
  and had dominated long prompts single-threaded).
- Parity tests: chunked prefill matches sequential logits and KV state on single-chunk,
  multi-chunk, and ragged-tail prompts.

### 7. Q4_K → Q8_0 repack (`eb25b34`) — the big one

Prefill profiling buckets (added to `step_chunk` this session) showed the down
projection eating 65% of prefill. An isolated microbenchmark (`bench_matmul_shapes`)
proved the kernel ran the same shape at full speed, and a per-layer trace found a
layer-stable bimodal split: 1.6ms vs 25–40ms. Cause: `ffn_down` is Q4_K on roughly half
the layers in this GGUF (its 4864-wide rows divide by 256; the 896-wide tensors can't be
Q4_K), and Q4_K had no integer kernel — those layers fell into the f32 bridge, which
re-dequantizes every weight block once per batch row.

Q4_K now repacks to Q8_0 at load like Q5_0/Q6_K (~0.4% requantization error, the same
class as the already-accepted Q6_K repack). Down went 11.5ms → 1.6ms per prefill token,
and decode's down matvec also left the bridge (7.3 → ~5ms).

Also landed here: packed activation panels (`row_dot_packed8`, block-interleaved quants
and scales — one sequential activation stream instead of up to 17), thread-local pack
scratch (a fresh `Vec` per call was taking demand-zero page-fault stalls under memory
pressure), L1-aware group tiers in the unpacked fallback, and a `GLPROC_NO_LOCK=1` A/B
knob for the VirtualLock working-set resize (measured neutral).

### 8. Quantized embedding + parallel load (`6257088`)

- `GlprocModel.token_embd` is now a `WeightMatrix` (Q8_0 post-repack when possible);
  `embed_into` dequantizes one row per lookup (sub-microsecond). Saves the ~4× f32
  blow-up: −500 MB RAM on the 0.5B, −933 MB would-be on the 1.5B. Tied-head models reuse
  the quantized table as the LM head on the integer-dot path.
- Layer tensors build in parallel across cores (shared atomic index counter, per-slot
  results): weights phase 1.41s → ~0.7s.
- `[load] tokenizer X | weights Y | pin Z` printed on every load.
- Lazy per-layer chase evaluated and skipped: the first prefill chunk needs all layers,
  so time-to-first-token stays build-bound; at 0.9s load the overlap would hide ~0.2s.

---

## Benchmarks (this session's end state)

Qwen2.5-0.5B-Instruct Q4_K_M, quiet machine, Defender excluded, 3 runs:

| Metric | Session start | Session end | llama.cpp b9888 |
|---|---|---|---|
| Prefill (133-token prompt) | ~35 tok/s (sequential) | **128–132 tok/s** | 124.5 (pp24) |
| Prefill (24-token chat prompt) | — | 116–148 tok/s | — |
| Generation (128 tokens) | ~23 tok/s inflated / ~15 honest | **33.5–35.2 tok/s** | 39.0 (tg128) |
| Load time | ~2.6s | **0.9s** | ~1–2s |
| Peak working set | ~1.7 GB | **1.19 GB** | — |
| Qwen2.5-1.5B generation | 5.3 tok/s | **12.1 tok/s** | — |
| Workspace tests | 435 | **459** | — |

Decode profile per token (best clean run): `qkv 2.4 | attn 2.0 | wo 1.6 | gateup 9.5 |
down ~5 | lm_head 5.5 | sampler 1.0` ms. Gate/up streams at ~23.4 GB/s ≈ this box's
practical DRAM limit; decode is memory-bandwidth-bound, which is why it sits at ~90% of
llama.cpp rather than above it.

## Benchmark Hygiene (hard-won)

- **Windows Defender** rescans the exe and GGUF after every build; benchmarks collapse
  2–4× while MsMpEng runs. Exclusions for the workspace and model folder are now set.
- **rust-analyzer** reindexes after every `cargo build` — wait for CPU < 15% before
  measuring (`powershell -NoProfile "(Get-CimInstance Win32_Processor).LoadPercentage"`).
- The user's PowerShell profile prints a banner that corrupts captured output — always
  `-NoProfile` in scripts.
- `llama-cli` hangs in conversation mode when driven non-interactively; use `llama-bench`.
